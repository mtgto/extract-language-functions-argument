RSpec.describe Extract::Language::Functions::Argument do
  it "has a version number" do
    expect(Extract::Language::Functions::Argument::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
